import React from "react";
// need to re direct to login page if not logged in - useAuth status hook
const NewTicket = () => {
  return (
    <div>
      <h1>new</h1>
    </div>
  );
};

export default NewTicket;
